{
  "name": "bufood",
  "version": "1.0.0",
  "description": "BuFood Application",
  "scripts": {
    "start": "concurrently \"cd backend-api && npm run dev\" \"cd frontend && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "axios": "^1.10.0",
    "recharts": "^3.0.2"
  }
}
